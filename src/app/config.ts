"use strict";

var config: any = {
  // default account name for the first run (without leading @)
  default_account: "nafestw",

  // if true, display a link to the last post of the
  // account set above in default_account
  show_promo_link: false
};

export default config;
